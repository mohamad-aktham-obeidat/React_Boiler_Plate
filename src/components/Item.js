import React, {
    Component
} from 'react';

export default class Item extends Component {
    state = {

    };

    render() {
        return (
             <>
                <h1>Item component</h1>
            </ >
        );
    }
}